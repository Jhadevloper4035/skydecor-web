<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Specification - Skydecor</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700;800&display=swap');

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        @page {
            size: A4;
            margin: 0;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            background: #ffffff;
            width: 210mm;
            height: 297mm;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
        }

        /* Header with Logo */
        .pdf-header {
            background: linear-gradient(135deg, #1a252f 0%, #2c3e50 100%);
            padding: 25px 40px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 4px solid #e67e22;
            margin-bottom: 30px;
        }

        .logo-section {
            flex: 1;
        }

        .logo {
            font-size: 36px;
            font-weight: 800;
            color: #ffffff;
            letter-spacing: 4px;
            text-transform: uppercase;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .logo-tagline {
            font-size: 10px;
            color: #bdc3c7;
            font-weight: 400;
            letter-spacing: 2px;
            text-transform: uppercase;
            margin-top: 3px;
        }

        .header-label {
            background: #e67e22;
            color: #ffffff;
            padding: 10px 20px;
            font-size: 12px;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
            border-radius: 5px;
        }

        /* Product Image Section */
        .product-image-section {
            text-align: center;
            margin-bottom: 30px;
        }

        .product-title {
            font-size: 26px;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 20px;
            letter-spacing: 0.5px;
        }

        .product-image {
            width: 220px;
            height: 350px;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
            border: 3px solid #ecf0f1;
            display: inline-block;
        }

        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Specifications Table */
        .specs-container {
            flex: 1;
        }

        .specs-title {
            background: linear-gradient(135deg, #2c3e50 0%, #34495e 100%);
            padding: 15px 30px;
            color: #ffffff;
            font-size: 16px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            border-radius: 8px 8px 0 0;
            text-align: center;
        }

        .specs-table {
            width: 100%;
            border-collapse: collapse;
            background: #ffffff;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
        }

        .specs-table tr {
            border-bottom: 1px solid #ecf0f1;
        }

        .specs-table tr:last-child {
            border-bottom: none;
        }

        .specs-table th {
            background: #f8f9fa;
            padding: 14px 30px;
            font-size: 12px;
            font-weight: 700;
            color: #2c3e50;
            text-align: left;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            width: 45%;
            border-right: 3px solid #e67e22;
        }

        .specs-table td {
            background: #ffffff;
            padding: 14px 30px;
            font-size: 13px;
            color: #555555;
            font-weight: 500;
            text-align: left;
        }

        .specs-table tr:nth-child(even) th {
            background: #ecf0f1;
        }

        .specs-table tr:nth-child(even) td {
            background: #fafafa;
        }

        /* Footer */
        .pdf-footer {
            margin-top: auto;
            padding: 20px 50px;
            border-top: 2px solid #ecf0f1;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: #7f8c8d;
            font-size: 10px;
            background: linear-gradient(135deg, #1a252f 0%, #2c3e50 100%);
            color: #fff !important;
        }

        .footer-left {
            display: flex;
            gap: 15px;
        }

        .footer-right {
            font-weight: 600;
            color: #fff;
        }

        /* Print Optimization */
        @media print {
            body {
                width: 210mm;
                height: 297mm;
            }
        }
    </style>
</head>

<body>
    <!-- Header -->
    <div class="pdf-header">
        <div class="logo-section">
            <div class="logo">SKYDECOR</div>
            <div class="logo-tagline">Own your spaces</div>
        </div>
        <div class="header-label">Product Specification</div>
    </div>

    <!-- Product Image Section -->
    <div class="product-image-section">
        <h1 class="product-title">
            <%= product.productCode || product.productName %>
        </h1>
        <div class="product-image">
            <img src="<%= product.image %>" alt="<%= product.designName || product.productName %>" />
        </div>
    </div>

    <!-- Specifications Table -->
    <div class="specs-container" style="padding: 10px 50px;">
        <div class="specs-title">Product Specifications</div>
        <table class="specs-table">
            <tr>
                <th>Product Code</th>
                <td>
                    <%= product.productCode %>
                </td>
            </tr>
            <tr>
                <th>Product Name</th>
                <td>
                    <%= product.productName || "N/A" %>
                </td>
            </tr>
            <tr>
                <th>Design Name</th>
                <td>
                    <%= product.designName || "N/A" %>
                </td>
            </tr>
            <tr>
                <th>Category</th>
                <td>
                    <%= product.category || "N/A" %>
                </td>
            </tr>
            <tr>
                <th>SubCategory</th>
                <td>
                    <%= product.subCategory || "N/A" %>
                </td>
            </tr>
            <tr>
                <th>Texture</th>
                <td>
                    <%= product.texture || "N/A" %>
                        <% if (product.textureCode) { %> (<%= product.textureCode %>)<% } %>
                </td>
            </tr>
            <tr>
                <th>Size</th>
                <td>
                    <%= product.size || "N/A" %>
                </td>
            </tr>
            <tr>
                <th>Thickness</th>
                <td>
                    <%= product.thickness || "N/A" %>
                </td>
            </tr>
            <tr>
                <th>Width</th>
                <td>
                    <%= product.width || "N/A" %>
                </td>
            </tr>
        </table>
    </div>

    <!-- Footer -->
    <div class="pdf-footer">
        <div class="footer-left"
            style="width: 100%; justify-content: space-evenly; align-items: center; font-size: 12px;">
            <span>www.skydecor.in</span>
            <span>|</span>
            <span>info@skydecor.in</span>
            <span>|</span>
            <span>+91 8800777731</span>
            <span>|</span>
            <span> © 2025 Skydecor. All Rights Reserved. </span>

        </div>
    </div>
</body>
</html>